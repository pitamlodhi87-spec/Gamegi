# ChessPro (Gamegi)

Simple React chess app with local 2-player and single-player vs AI.

How to run:
1. npm install
2. npm run dev
3. Open http://localhost:5173

Then commit these files to your repo (Gamegi) and push.

Suggested git commands:
